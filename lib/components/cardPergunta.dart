import 'package:flutter/material.dart';

class Cardpergunta extends StatelessWidget {
  final String texto;
  
  const Cardpergunta({Key? key, required this.texto}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
       
      ],
    );
  }
}
